# ProfileSmith 1.3.0

- 优化 Finder“打开方式 -> ProfileSmith”处理逻辑：已存在的描述文件会直接在左侧列表中定位并选中，不存在的描述文件会自动导入后选中并滚动到可见区域。
- 修复右侧预览页右键菜单仍会出现 Reload、点击后预览内容变空白的问题。
- 新增“平台”列，并修复列表表头再次点击无法反向排序的问题。
- 修复“状态”列表头异常与最右侧多余分隔线，调整左侧 TableArea 拖拽最小宽度与“名称”列最小宽度保持一致。
- 修复通过外部文件拉起应用时主窗口没有主动切到前台的问题，并补强对应回归测试。
